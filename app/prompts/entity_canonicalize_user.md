Map these extracted entity names to existing slugs.

Extracted names:
{{extracted_names}}

Existing slugs (slug → display name):
{{existing_slugs}}

Return JSON only. Example: {"Project Alpha": "project-alpha", "unknown thing": null}
